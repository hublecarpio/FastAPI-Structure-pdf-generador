<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <title>{{titulo}}</title>

  <style>
    /* CONFIGURACIÓN BASE */
    @page {
      size: A4;
      margin: 22mm 25mm 22mm 25mm;
    }

    body {
      font-family: 'Helvetica', 'Arial', sans-serif;
      color: #1e1e1e;
      line-height: 1.65;
      font-size: 12.5pt;
      background-color: #fff;
      margin: 0;
      padding: 0;
    }

    /* CABECERA */
    header {
      text-align: center;
      margin-bottom: 40px;
      padding-top: 20px;
    }

    .logo {
      width: 80px;
      margin-bottom: 10px;
      opacity: 0.9;
    }

    h1 {
      font-size: 24pt;
      color: #003366;
      margin-bottom: 6px;
      line-height: 1.2;
    }

    .autor {
      font-size: 11.5pt;
      color: #555;
      margin-top: 0;
    }

    .fecha {
      font-size: 10.5pt;
      color: #777;
      margin-top: 3px;
    }

    /* SECCIONES */
    main {
      max-width: 160mm;
      margin: 0 auto;
    }

    .seccion {
      margin-bottom: 35px;
    }

    h2 {
      font-size: 16.5pt;
      color: #004C99;
      border-left: 5px solid #007ACC;
      padding-left: 10px;
      margin-bottom: 15px;
      line-height: 1.3;
    }

    p {
      text-align: justify;
      margin-bottom: 10px;
    }

    ul, ol {
      margin: 0 0 10px 25px;
    }

    li {
      margin-bottom: 5px;
    }

    strong {
      color: #003366;
    }

    em {
      color: #555;
    }

    /* BLOQUE RESUMEN */
    .resumen {
      background: #f7f9fc;
      border: 1px solid #d9e2ef;
      border-left: 5px solid #007ACC;
      padding: 18px 20px;
      border-radius: 10px;
    }

    .resumen h2 {
      border: none;
      color: #003366;
      padding-left: 0;
      margin-top: 0;
    }

    /* PREVENIR CORTES FEOS */
    h1, h2, h3 {
      page-break-after: avoid;
    }

    p, ul, ol {
      page-break-inside: avoid;
    }

    /* ESPACIADO ENTRE PÁRRAFOS LARGOS */
    .seccion p + p {
      margin-top: 6px;
    }

    /* OPCIONAL: SALTO DE PÁGINA ENTRE SECCIONES MUY LARGAS */
    .page-break {
      page-break-before: always;
    }
  </style>
</head>
<body>

  <header>
    <img src="https://cdn-icons-png.flaticon.com/512/565/565547.png" alt="logo" class="logo">
    <h1>{{titulo}}</h1>
    <div class="autor">Autor: {{autor}}</div>
    <div class="fecha">Fecha: {{fecha}}</div>
  </header>

  <main>
    <section class="seccion">
      <h2>Introducción</h2>
      {{{introduccion}}}
    </section>

    <section class="seccion page-break">
      <h2>Desarrollo</h2>
      {{{desarrollo}}}
    </section>

    <section class="seccion resumen page-break">
      <h2>Resumen Final</h2>
      {{{resumen}}}
    </section>
  </main>

</body>
</html>