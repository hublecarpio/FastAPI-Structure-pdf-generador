# ============================================
# CONFIGURACION PARA TU SERVIDOR UBUNTU
# Copia este archivo como .env y llena los valores
# ============================================

# PUERTO DE LA APLICACION
# Cambia al puerto que tengas disponible
APP_PORT=3000

# CLAVE SECRETA PARA JWT (OBLIGATORIO)
# Genera una clave larga y aleatoria, minimo 32 caracteres
# Puedes generar una con: openssl rand -hex 32
SESSION_SECRET=CAMBIAR_ESTO_genera_una_clave_con_openssl_rand_hex_32

# CONEXION A TU POSTGRESQL (OBLIGATORIO)
# Formato: postgresql://USUARIO:PASSWORD@HOST:PUERTO/NOMBRE_DB
# 
# Ejemplo si tu PostgreSQL esta en el mismo servidor:
# DATABASE_URL=postgresql://pdfuser:mipassword123@localhost:5432/pdfapi
#
# Ejemplo si usas el usuario postgres directamente:
# DATABASE_URL=postgresql://postgres:tupassword@localhost:5432/pdfapi
#
DATABASE_URL=postgresql://USUARIO:PASSWORD@localhost:5432/pdfapi

# ============================================
# OPCIONALES - S3 para almacenar templates
# Si no los configuras, usa almacenamiento local
# ============================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
S3_BUCKET=pdf-templates
